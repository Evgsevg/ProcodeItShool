const oldYear=require('./compare.js');
const mycurrentdateBirthd=oldYear.resyear;

//console.log(mycurrentdateBirthd+'++++++++++');
/*
const jobs = []
const interval = setInterval(() => {
	if(jobs.length == 10) {
		clearInterval(interval);
	}
	let job = Promise.resolve('вам'+'  '+ mycurrentdateBirthd+ "  "+', вы стали на 10 сек старше');
	jobs.push(job);
	console.log('Вы стали на 10 сек старше')
}, 10000);

setTimeout(() => {
	Promise.all(jobs).then(data => console.log(data))
}, 10000*15);

*/



console.log("Module two connected!");
