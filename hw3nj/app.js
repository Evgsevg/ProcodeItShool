require('./mymodules');
//const { DateTime } = require("luxon");
/*
const moment = require('moment');
moment.locale('ru');

const many_years_ago= moment(showdate,"YYYYMMDD").fromNow();

const showdate=moment(MyDateBirhd).format();
*/
/*
const MyDateBirhd =
{
    y:1996,
    d:13,
    m:11
}
console.log(MyDateBirhd);
let add_minutes =  function (dt, minutes) {
    return new Date(dt.getTime() + minutes*60000);
}
console.log(add_minutes(new Date(2014,10,2), 10).toString());
DateTime.now().setZone("America/New_York").minus({ weeks: 1 }).endOf("day").toISO();
*/